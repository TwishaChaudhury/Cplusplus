#include<bits./stdc++.h>
using namespace std;
int Array[1000];
int Auxiliary_Array[1000];
int merge_array[1000];
void merge_array(int low,int mid,int high){
    //int i=low; //1st array
   // int j= mid+1; //2nd array
    //int k= low ;//auxilary_array
    scanf("%d%d",&low,&mid+1);
    int Array_A[low];
    int Array_B[mid+1];
    for(int i=0;i<low;i++){

        scanf("%d",&Array_A[i]);
    }
    for(int i=0;i<mid+1;i++){

        scanf("%d",&Array_B[i]);
    }
    int Array_C[low]
    while(i<=low && j<=high){
        Auxiliary_array[k]=Array[i];
    }





}
